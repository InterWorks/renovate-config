# renovate-config

InterWorks's default Renovate config. Extend via `"extends": ["github>InterWorks/renovate-config"]` in your repo's `renovate.json`.

Sets a 7-day cooldown on new releases (supply-chain hardening) and applies Renovate's `config:best-practices` preset. Maintained by @henryjarend and @interworks-morr.
